# Roles Module

Manages roles and displays access matrix.

## Structure

- `components/` - Role list, Access matrix components
- `services/` - Role service

## Routes

- `/roles` - List all roles
- `/roles/access-matrix` - Display access matrix (permissions per role)
